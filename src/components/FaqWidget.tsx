import WidgetCard from "./WidgetCard";

export default function FaqWidget() {
  return (
    <WidgetCard
      title="FAQ"
      subtitle="Les réponses aux questions fréquentes"
      href="/faq"
       // optionnel — mets une image si tu veux
    />
  );
}
